streamlit>=1.30
anthropic>=0.40
pandas>=2.0
openpyxl>=3.1
pdfplumber>=0.10
python-dotenv>=1.0

# Procesamiento de documentos
python-docx>=1.0        # Word (.docx)
beautifulsoup4>=4.12    # HTML
pymupdf>=1.23           # PDFs escaneados (renderiza páginas como imagen)

# Procesamiento de imágenes
pillow>=10.0            # Conversión de formatos
pillow-heif>=0.13       # HEIC/HEIF (fotos de iPhone)

# Integración Gmail (lectura de hilos + envío de mails)
# Requiere configurar data/gmail_credentials.json (ver services/gmail_service.py)
google-auth>=2.0
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2
google-api-python-client>=2.0
